//This testfile checks the functioning of the library functions printi, readi and prints

int printi(int num);
int prints(char * c);
int readi(int *eP);

int main() {
    int c[10],a=13;
    c[2]=100;
    printi(a);
    return 0;
}